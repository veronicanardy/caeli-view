/**
 * Modos visuais suportados pelo observatório.
 *
 * Este tipo fica isolado para evitar acoplamento entre o shell do modal,
 * os blocos do manual e os demais controles da cena.
 */
export type SceneMode = 'radar' | 'orbit';
